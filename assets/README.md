Runtime assets belong here: GLB models, optimized textures, and audio. These files are copied unchanged to dist. The training ground is still procedural in src/levels/trainingGround.ts. First-person arms (`models/pmc-arms.glb`) and the service carbine (`models/service-carbine.glb`) are original exports from `art/tools/`. Track asset sources and licenses when adding assets.
